[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ag2-webzio"
version = "0.1.0"
description = "AG2 tools for Webz.io News Search - global news in natural language with semantic ranking and rich filters"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE"]
authors = [
    { name = "Webz.io", email = "support@webz.io" },
]
keywords = ["ag2", "autogen", "webz", "webzio", "news", "mcp", "search"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "ag2[mcp]>=1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
]

[project.urls]
Homepage = "https://news-search-mcp.webz.io"
Documentation = "https://docs.webz.io/docs/webz/news-search-api-mcp"
Repository = "https://github.com/Webhose/webz-news-search"
Issues = "https://github.com/Webhose/webz-news-search/issues"

[tool.hatch.build.targets.wheel]
packages = ["ag2_webzio"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]
